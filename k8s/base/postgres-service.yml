apiVersion: v1
kind: Service
metadata:
  name: service-postgres
  namespace: django-app
spec:
  type: ClusterIP
  selector:
    app: django
    component: db
  ports:
    - port: 5432
      targetPort: 5432

