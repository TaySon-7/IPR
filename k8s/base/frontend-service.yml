apiVersion: v1
kind: Service
metadata:
  name: service-frontend
  namespace: django-app
spec:
  type: LoadBalancer
  selector:
    app: django
    component: frontend
  ports:
    - name: http
      port: 3000
      targetPort: http
