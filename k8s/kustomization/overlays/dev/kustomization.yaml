apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: django-dev

resources:
  - ../../base
  - configmap.yaml
  - secrets.yaml

patches:
  - target:
      kind: Deployment
      name: django
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 1
