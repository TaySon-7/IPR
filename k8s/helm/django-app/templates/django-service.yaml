apiVersion: v1
kind: Service
metadata:
  name: service-django
  namespace: {{ .Values.namespace }}
spec:
  type: LoadBalancer
  selector:
    app: django
    component: backend
  ports:
    - port: 8000
      targetPort: 8000

